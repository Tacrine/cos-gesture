# Gradle / Android build outputs
.gradle/
build/
app/build/
local.properties
*.apk
*.aab

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Device artifacts (pulled APKs, jars, logcat, getevent) - never commit binaries
*.dex
*.odex
*.vdex
*.oat

# Session / local
*.log
captures/

# OMO tooling: fully local — plans/evidence/screenshots are work artifacts, not source
.omo/
.kotlin/

# Reverse-engineered / decompiled artifacts (never commit)
apk_extract/

# Local summary drafts
/summary*.txt
*gesture-summary*.txt
# Reverse-engineering / decompile dumps (large text artifacts, never commit)
apk_extract/
