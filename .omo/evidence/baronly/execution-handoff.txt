Execution handoff notes (Copilot session 338bb515, stopped mid task 3 at user request)

Completed:
- Task 1 (evidence task-1-offbar-block.txt): FAILURE-B on all 4 off-bar gestures.
  VETO barOnly down IS logged, but handleNormalGestureEnd still fires and focus
  changes -> SideGestureDetector#onMotionEventImpl DOWN veto does NOT block bottom
  gestures. => Task 5 (fallback interception layer) IS TRIGGERED.
- Task 2 (evidence task-2-inband-pass.txt): in-band 3/3 pass (PASS down + HOME/RECENTS end).
- Step-0 injection control passed: input swipe reaches hook (PASS down x=540 + HOME end).

Key operational facts for the executor:
1. barOnly was initially OFF (barOnly=0) - always `content query` config BEFORE testing.
   Toggle path: launch com.cos.lspit.gesture/.ui.MainActivity, uiautomator dump,
   switch column x=[885,1032]; mBack row y~1782-1926, barOnly row y~1962-2106
   (SHIFT when the list scrolls - always re-dump before tapping).
2. Slider (白条长度) enabled = !mbackEnabled. To change width: toggle mBack off first,
   drag slider, re-enable. SeekBar bounds ~[18,2202][1062,2334].
3. UI navigation hazard: horizontal swipes inside the app can flip to an error page;
   tap bottom-nav 首页 [180,2184][473,2328] to return.
4. After am force-stop + relaunch, provider and UI agree (no multi-process issue).
   Mid-session they disagreed because taps landed on the wrong (scrolled) rows.
5. Device state at handoff: master=1 left=1 right=1 mback=1 barWidthDp=40 barOnly=0.
   barOnly left OFF after testing - re-enable before further gate tests.
6. Task 5 research direction: off-bar veto executes but AbsSwipeUpHandler still
   completes; the real fix must stop the gesture earlier (InputDispatcher-level
   homegesture interception region y>=2310 full-width, or hook the handler's
   event source), not the detector veto. Reference logcat:
   InputDispatcherExtImpl ... homegesture_displayid_0.
