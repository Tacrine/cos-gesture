Launch scope change for barOnly 方案 A
Date: 2026-09-08
Trigger: task-5-fallback.txt — bottom (homegesture) region owned by LAUNCHER.
Decision: option A — extend LSPosed scope to com.android.launcher, hook
  android.view.OplusWindowManager.updateInvalidRegion in launcher process.

=== scope.list (module APK resource) ===
app/src/main/resources/META-INF/xposed/scope.list now contains:
  com.android.systemui
  com.android.launcher
(APK rebuilt + adb install -r successful. NOTE: LSPosed does NOT auto-apply
 scope.list change — Manager app is the normal path to rewrite modules_config.db;
 no LSPosed Manager installed on device => manual DB edit below.)

=== modules_config.db offline edit (no sqlite3 CLI on device) ===
DB: /data/adb/lspd/config/modules_config.db
Steps:
  1. adb pull db + -wal + -shm to PC (WAL mode; pull all 3 to recover latest rows).
  2. Open with Python sqlite3 (auto-recovers WAL), 
       INSERT OR IGNORE INTO scope(module_pkg_name,app_pkg_name,user_id)
         VALUES('com.cos.lspit.gesture','com.android.launcher',0);
     verify modules_state user 0 enabled=1 for module.
  3. cur.close() then VACUUM INTO single-file copy (cannot VACUUM with open stmts;
     cannot rm -wal on Windows - file lock WinError 32).
  4. push copy to /data/local/tmp, su -c 'dd if=... of=/data/adb/lspd/config/
     modules_config.db', rm -f db-wal db-shm, chmod 666 both.
  5. adb reboot — LSPosed reads new scope on boot.

Result rows after edit (user 0):
  scope: com.cos.lspit.gesture -> com.android.launcher, com.android.systemui
Verification pending post-boot: module must load in launcher process
  (logcat: MODULE_READY launcher / HOOK_REGISTERED android.view.OplusWindowManager#updateInvalidRegion).
